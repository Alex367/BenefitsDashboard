# Start all tests in headless mode

``` bash
npx playwright test
```

# Start all ui tests in headless mode

``` bash
npx playwright test ui/
```

# Start all api tests in headless mode

``` bash
npx playwright test api/
```